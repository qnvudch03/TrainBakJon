#include <iostream>

int main() {
    unsigned char a, b;

    while (true) {
        std::cin >> a >> b;
        if (a == 0 && b == 0) break;
        std::cout << (int)(a + b) << '\n';
    }

    return 0;
}