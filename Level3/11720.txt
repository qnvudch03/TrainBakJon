#include <iostream>
#include <string>

int main() {
    int n, sum = 0;
    std::string s;

    std::cin >> n;
    std::cin >> s;

    for (int i = 0; i < n; i++) {
        sum += s[i] - '0';
    }

    std::cout << sum;
}
